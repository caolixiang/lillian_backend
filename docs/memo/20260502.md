# 20260502 Memo

## Items

- [13:55] Installed coding memo workflow files and created today's plan/memo.
- [14:00] Confirmed user approval to commit workflow/plan docs and create an isolated worktree for `plan.md`.
- [14:00] Added `.worktrees` to `.gitignore` and verified it is ignored.
- [14:01] Committed workflow and wallet entitlement implementation plan docs on `main`.
- [14:02] Created `.worktrees/wallet-entitlements` on branch `wallet-entitlements`.
- [14:03] Ran baseline `go test ./...` in the worktree; baseline passed.
- [14:04] Added RED wallet API tests for create, restore, get, and recovery hash storage.
- [14:05] Implemented Phase 1 wallet schema and API handlers.
- [14:06] Ran wallet and DB package tests, then full `go test ./...`; all passed.
- [14:07] Committed Phase 1 implementation and updated daily plan/memo.
- [14:10] Started Phase 2 by reviewing license/admin patterns and writing wallet redemption RED tests.
- [14:12] Implemented `/api/wallets/redeem`, license redemption columns, and wallet entitlement upsert semantics.
- [14:15] Updated admin license generation/listing for service codes and redeemed wallet display.
- [14:17] Verified Go tests and admin build; reran Go tests after avoiding concurrent admin_dist deletion during build.
- [14:18] Committed Phase 2 implementation and updated daily plan/memo.
- [14:24] Started Phase 3 by reviewing task creation, worker execution, provider selection, and refund paths.
- [14:27] Added RED tests for wallet service selection and task wallet accounting migration.
- [14:31] Implemented wallet-backed task reservation, service selection, charged flag, and failure release behavior.
- [14:33] Verified Phase 3 with `go test ./...` and `git diff --check`.
- [14:34] Committed Phase 3 implementation and updated daily plan/memo.
- [14:43] Continued Phase 4 from the RED admin wallet lookup test.
- [14:45] Implemented `/admin/wallets/{address}` with wallet entitlements, redemption history, and recent task summaries.
- [14:47] Added the admin UI wallet tab with address lookup, balance metrics, redemptions, and task tables.
- [14:48] Verified Phase 4 with `go test ./internal/httpapi`, `go test ./...`, `npm run build` in `web/admin`, and `git diff --check`.
- [14:48] Committed Phase 4 implementation and updated daily plan/memo.
- [14:50] Started Phase 5 by confirming this repo has backend/admin frontend only and no user-side SPA source.
- [14:52] Added a RED task response contract test for wallet snapshot and accounting fields needed by frontend polling.
- [14:53] Implemented task response wallet snapshot/accounting fields and documented the wallet frontend API flow in README.
- [14:54] Verified Phase 5 with `go test ./internal/httpapi`, `go test ./...`, `npm run build` in `web/admin`, and `git diff --check`.
- [14:54] Committed Phase 5 implementation and updated daily plan/memo.
- [15:03] Evaluated review feedback; kept legacy activation endpoints for compatibility and selected stale running task recovery plus HD label cleanup for immediate fixes.
- [15:08] Added RED tests for user-facing HD labels and stale running wallet task reservation recovery.
- [15:12] Implemented startup stale running wallet task recovery with atomic task marking and entitlement refund.
- [15:14] Verified review fixes with `go test ./internal/httpapi`, `go test ./...`, and `git diff --check`; merged the fix to `main`.
- [15:27] Merged wallet performance optimization `f747383` to `main` after user selected Postgres indexes, read reduction, and worker DB pressure tuning.
- [15:27] Verified the merged optimization on `main` with `go test ./...` and `git diff --check`.
- [15:35] Documented `TASK_WORKER_CONCURRENCY` in env examples, README, and deployment notes with `8` as the initial 8 vCPU / 8 GB VPS production starting point.
- [15:39] Revised the 8 vCPU / 8 GB VPS guidance to `16` after confirming Postgres runs on Railway separately and workers do not hold DB transactions during upstream generation.
- [15:43] Added RED tests for pgxpool max/min ENV configuration, then implemented `DB_POOL_MAX_CONNS` and `DB_POOL_MIN_CONNS` through config loading and `db.Open`.
- [15:43] Added pgxpool max/min settings to local `.env`, env examples, README, and deployment notes with production guidance of max `32` and min `4`.
- [16:05] Removed the legacy activation-token API path, direct license credit accounting, legacy task/license fields, and admin UI fallback fields after the user confirmed compatibility is not required.
- [16:07] Verified Phase 9 with `go test ./...`, `npm run build` in `web/admin`, a second `go test ./...`, `git diff --check`, and a legacy keyword scan.
- [18:02] Fixed the wallet redemption migration backfill order, reset the local `.env` Postgres database by dropping/recreating `public`, reran all migrations, and verified backend/admin checks.

## Commits

- `7facf7e`: Add wallet entitlement implementation plan
- `ee2d805`: Add wallet create restore APIs
- `d420a5c`: Redeem license codes into wallet entitlements
- `fbaf513`: Bill generation tasks against wallet entitlements
- `33ee29c`: Add admin wallet lookup
- `a0be277`: Stabilize wallet task frontend responses
- `3035d8f`: Recover stale wallet task reservations
- `f747383`: Optimize wallet task hot paths
- `de9c4ba`: Expose postgres pool tuning
- `a59eaa7`: Remove legacy activation token flow
- `e93e0ef`: Fix wallet migration backfill order

## Notes

- User confirmed starting `plan.md` with SuperPowers.
- Code-review subagent workflow was not used because this session's higher-priority tool instruction requires explicit user authorization for subagents.
- Do not run `go test ./...` concurrently with `web/admin` build because the build command removes embedded admin assets before regenerating them.
- Phase 3 kept existing task status names for compatibility while adding wallet accounting fields.
- Phase 4 build regenerated ignored `internal/httpapi/admin_dist` assets locally; the code commit contains source and tests only.
- Phase 5 did not modify a user-side SPA because this repository does not contain one; it stabilized and documented the backend contract the SPA will consume.
- Review follow-up kept `.codex/` and same-day plan/memo uncommitted; only business-code review fixes were committed.
- Redis remains deferred for now; the performance work keeps wallet balances/accounting authoritative in Postgres and targets indexes, query shape, and worker concurrency first.
- `TASK_WORKER_CONCURRENCY` is startup ENV and is separate from `/admin` runtime image concurrency; for the external-Postgres VPS deployment, start at `16`, then test `24` and later `32` with DB connection wait, provider throttling, and storage latency observations.
- pgxpool ENV surface intentionally includes only `DB_POOL_MAX_CONNS` and `DB_POOL_MIN_CONNS`; leave lifetime/health-check values on library defaults unless production metrics show a reason to tune them.
- Phase 9 leaves `wallet_redemptions.license_key_id` in place because it is wallet redemption history, not an activation-token credential.
- The local `.env` database was intentionally cleared with `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` before rerunning migrations because compatibility with existing local data is not required.
