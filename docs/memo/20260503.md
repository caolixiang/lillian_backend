<!-- generated-by: my-coding-memo-skill -->

# 20260503 Memo

## Items

- [11:53] Resumed after context compaction, read the May 2 plan/memo, and confirmed the repo was clean on `main`.
- [11:53] Reviewed current credit pricing implementation: migrations seed `image-2-hd` rows for `1K`, `2K`, `4K`, while task billing maps the requested size directly to `billing_key`.
- [11:53] User approved simplifying the admin credit pricing UI so only current business-backed price controls remain editable.
- [12:12] Added RED tests for unified HD billing keys, supported credit price guards, and the HD pricing migration.
- [12:18] Implemented unified `HD` credit billing for 2K/4K, added an existing-deployment migration to create `image-2-hd / HD` and disable legacy HD size rows, and constrained admin API/listing to supported image prices.
- [12:25] Updated the admin UI to use a preset credit pricing selector instead of free-form service code and billing key inputs.
- [12:31] Verified with targeted Go tests, `go test ./...`, `npm run build` in `web/admin`, a second `go test ./...`, `go vet ./...`, and `git diff --check`.
- [12:43] User asked to add only the backend public top-up plan listing API so their frontend can choose packages by returned `planId`.
- [12:46] Added RED tests for `GET /api/topup-plans` route registration and frontend-safe top-up plan serialization.
- [12:50] Implemented `GET /api/topup-plans`, returning enabled plans as public fields only while keeping admin billing settings on full admin fields.
- [12:53] Verified the public top-up plan API with targeted tests, `go test ./...`, `go vet ./...`, and `git diff --check`.
- [12:58] User approved moving service provider, credit price, and top-up plan create/edit forms into modal dialogs.
- [13:06] Moved service provider, credit price, and top-up plan admin forms into a reusable modal shell and kept the main views focused on tables/actions.
- [13:10] Verified admin modal UI with `npm run build` in `web/admin`, `go test ./...`, and `git diff --check`; noted pre-existing `VERSION` change remains uncommitted and excluded.
- [15:15] Separated the admin Credits billing area into clearer price configuration and recharge package blocks, removed the standalone price configuration button, verified with admin build, Go tests, and diff check, and committed `0cba3f8`.

## Commits

- `bd1c09c`: Simplify image credit pricing
- `b4df4e1`: Expose public topup plans
- `1d12e4b`: Move admin forms into modals
- `0cba3f8`: Separate billing settings blocks

## Notes

- Current user-facing goal: `2K` and `4K` should share one HD credit price, and admin operators should not need to know or invent internal service codes.
