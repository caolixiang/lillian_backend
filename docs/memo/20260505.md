<!-- generated-by: my-coding-memo-skill -->

# 20260505 Memo

## Items

- [HH:mm] Item:
- [10:30] Started investigation of image edit failures for provider `mmzzcpa`; user-supplied upstream error says `input[0].content[1].image_url` used unsupported MIME type `application/octet-stream`.
- [10:30] Read the latest May 3 plan/memo and confirmed the worktree was clean before starting.
- [10:42] User clarified the affected HD providers are OhMyToken-compatible `sub2api` and `cpa/mmzzcpa`, not Responses mode; root cause narrowed to multipart image and mask file part content types.
- [10:49] Added a RED regression test showing image edit multipart `image[]` and `mask` parts were sent as `application/octet-stream`.
- [10:55] Replaced default multipart file part creation with explicit image part headers and MIME normalization, then verified targeted and full backend test suites.
- [11:01] Committed `16da7b1` for the Phase 1 code/test change.
- [11:18] Added a New API `images` mode mask edit guard proving the MIME fix preserves mask, output format, moderation, quality, and non-OhMyToken field behavior for normal providers.
- [11:22] Verified the new guard with targeted tests, full backend tests, full Go tests, vet, and diff check; committed `acc3175`.

## Commits

- `16da7b1`: Fix image edit multipart content types
- `acc3175`: Guard New API image edit masks

## Notes

- Do not store or echo the user-provided provider API key in repository files or logs.
