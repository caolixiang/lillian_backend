<!-- generated-by: my-coding-memo-skill -->

# Today's Goals

- Fix image edit requests for a provider that rejects `application/octet-stream` image data URLs.

## Scope

- In: Image edit input data URL MIME normalization, provider request payload tests, focused backend verification.
- Out: Provider credential changes, production database edits, frontend image upload behavior changes unless required by backend evidence.

## Phase Plan

### Phase 1 - Normalize Image Edit MIME Types

- Goal: Ensure image edit requests sent upstream use a valid `image/*` MIME type for base64 data URLs.
- Changes: Add a regression test for edit image inputs with missing or generic MIME metadata, then fix the backend normalization at the source.
- Acceptance: The regression test fails before implementation, passes after the fix, and focused backend tests pass.
- Status: done

### Phase 2 - Guard New API Mask Edits

- Goal: Prove the MIME fix does not break New API style image edits that include a mask.
- Changes: Add a regression guard for `APIMode: "images"` edit requests with `mask`, checking endpoint, multipart fields, and image part MIME headers.
- Acceptance: The New API mask test passes alongside the OhMyToken regression test and focused backend tests.
- Status: done

## Phase Acceptance Log

### Phase 1

- Result: Completed.
- Evidence: Image edit multipart parts for both `image[]` inputs and `mask` now explicitly carry normalized `image/*` content types instead of relying on Go's default `application/octet-stream` file-part header.
- Notes: The fix applies to all OhMyToken-compatible edit providers, including HD `sub2api` and `cpa/mmzzcpa` channels, without changing provider configuration.

### Phase 2

- Result: Completed.
- Evidence: Added a New API `images` mode edit test that verifies `/v1/images/edits`, `output_format=png`, `moderation=auto`, `quality=auto`, no OhMyToken-only `aspect_ratio`, and `image/png` MIME on both `image[]` and `mask`.
- Notes: This is a protection-only phase; no production code changed after Phase 1.

## Test Log

- `go test ./internal/httpapi -run TestImageEditMultipartUsesImageContentTypesForInputsAndMask` before implementation: failed as expected because `image[] content-type = "application/octet-stream"`.
- `go test ./internal/httpapi -run TestImageEditMultipartUsesImageContentTypesForInputsAndMask` after implementation: pass.
- `go test ./internal/httpapi`: pass.
- `go test ./...`: pass.
- `go vet ./...`: pass.
- `git diff --check`: pass.
- `go test ./internal/httpapi -run 'TestImageEditMultipartUsesImageContentTypesForInputsAndMask|TestNewAPIImageEditMultipartKeepsMaskAndImageOptions'`: pass after adding the New API mask guard; first run failed to compile because the new test needed the missing `io` import.
- `go test ./internal/httpapi` after Phase 2: pass.
- `go test ./...` after Phase 2: pass.
- `go vet ./...` after Phase 2: pass.
- `git diff --check` after Phase 2: pass.

## Commit Log

- `16da7b1` - Fix image edit multipart content types
- `acc3175` - Guard New API image edit masks

## Exception Log

- `requesting-code-review` subagent flow was not dispatched because the active Codex tool instruction only allows subagents when the user explicitly asks for delegation or parallel agent work.

## Tomorrow Plan

- Continue with deployment verification if requested.
