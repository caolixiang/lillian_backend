<!-- generated-by: my-coding-memo-skill -->

# Today's Goals

- Simplify wallet credit pricing so the admin UI exposes only business-backed image generation price controls.
- Add a public top-up plan listing API for the user-facing frontend.
- Move admin edit/create forms into modal dialogs to reduce persistent page clutter.

## Scope

- In: Image generation credit billing key mapping, seeded service credit prices, admin credit price settings UI, public top-up plan listing API, admin modal dialogs for service provider, credit price, and top-up plan forms, docs/tests for the simplified pricing model and top-up frontend contract.
- Out: New payment providers, recharge package pricing changes, user-side frontend implementation, new service categories such as TTS/video.

## Phase Plan

### Phase 1 - Simplify Image Credit Pricing

- Goal: Replace free-form image credit price configuration with the current supported business price rows.
- Changes: Map 2K/4K credit billing to a single HD key, seed/serve only supported image price rows by default, and update admin UI copy/forms to prevent arbitrary service-code entries.
- Acceptance: 2K and 4K credit billing both use one HD price; admin only lets operators edit meaningful price fields for supported image services; tests and admin build pass.
- Status: done

### Phase 2 - Public Top-Up Plan Listing

- Goal: Let the user-facing frontend discover enabled recharge packages instead of hard-coding plan IDs.
- Changes: Add `GET /api/topup-plans` returning enabled plans with public fields only; keep admin billing settings on the existing full admin payload.
- Acceptance: Public endpoint is registered, does not require admin auth, returns only frontend-safe plan fields, and existing top-up order creation still accepts `planId`.
- Status: done

### Phase 3 - Admin Form Modals

- Goal: Remove persistent service provider, credit price, and top-up plan edit forms from the page and open them only when needed.
- Changes: Add a reusable admin modal shell, move provider/credit/top-up forms into modal content, wire new/edit actions to open the correct modal, and close after successful saves.
- Acceptance: Service provider create/edit, credit price edit/create, and top-up plan create/edit still work; persistent tables are cleaner; admin build and Go embed verification pass.
- Status: done

## Phase Acceptance Log

### Phase 1

- Result: Completed.
- Evidence: Backend billing now maps 2K/4K credit usage to `HD`; admin credit price upserts reject unsupported service/billing keys; admin UI uses preset image price rows instead of free-form service-code inputs.
- Notes: 1K credit billing now uses the standard `image-2-sd / 1K` price even if generation later routes to an HD provider fallback.

### Phase 2

- Result: Completed.
- Evidence: `GET /api/topup-plans` is registered as a public API and returns `{ topupPlans: [{ id, label, amountUsdt, credits, isDefault }] }` from enabled plans.
- Notes: Admin billing settings continue to return full top-up plan fields including enabled/sort/note/timestamps.

### Phase 3

- Result: Completed.
- Evidence: Admin provider, credit price, and top-up plan forms now live in one reusable modal shell; lists/settings views keep only tables and action buttons.
- Notes: `VERSION` had a pre-existing uncommitted change and was intentionally excluded from this phase.

## Test Log

- `go test ./internal/httpapi -run 'TestCreditBillingForImageGenerationUsesConfiguredPrices|TestSupportedServiceCreditPriceOnlyAllowsBusinessBackedImageRows'`: pass after implementation; failed first because the HD key mapping and supported-price guard were missing.
- `go test ./internal/db`: pass after adding `0011_unify_hd_credit_pricing.sql`; failed first because the migration was missing.
- `go test ./...`: pass
- `npm run build` in `web/admin`: pass
- `go test ./...` after admin build: pass
- `go vet ./...`: pass
- `git diff --check`: pass
- `go test ./internal/httpapi -run 'TestPublicTopupPlansRouteExistsForFrontendSelection|TestPublicCreditTopupPlansExposeFrontendFieldsOnly'`: pass after implementation; failed first because the public route was missing and public serialization exposed admin fields.
- `go test ./...` after Phase 2: pass
- `go vet ./...` after Phase 2: pass
- `git diff --check` after Phase 2: pass
- `npm run build` in `web/admin` after Phase 3: pass
- `go test ./...` after Phase 3 admin build: pass
- `git diff --check` after Phase 3: pass

## Commit Log

- `bd1c09c` - Simplify image credit pricing
- `b4df4e1` - Expose public topup plans
- `1d12e4b` - Move admin forms into modals

## Exception Log

- SuperPowers spec/plan reviewer subagents were not dispatched because the active Codex tool instruction only allows subagents when the user explicitly asks for delegation or parallel agent work.
- Admin modal UI work uses TypeScript build and embed tests for verification because the existing admin frontend does not have a DOM/unit test runner.

## Tomorrow Plan

- Continue with deployment verification or payment gateway integration work as requested.
