services:
  backend:
    image: ghcr.io/your-org/lillian-backend:0.1.0
    restart: unless-stopped
    env_file:
      - path: ../.env
        required: false
    ports:
      - "${PORT:-8787}:${PORT:-8787}"
