services:
  backend:
    build:
      context: .
    image: lillian-backend:local
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    ports:
      - "${PORT:-8787}:${PORT:-8787}"
